1. **数据处理**: 创建 `src/data/neverHaveIEver.js`，将 `f:\Game\src\md\你有我没有.md` 中的题目整理为 JavaScript 数组并导出。
2. **页面开发**: 创建 `src/views/NeverHaveIEver.vue` 组件，实现“我有你没有”游戏逻辑（随机抽题、显示题目、切换题目）。
3. **路由配置**: 在 `src/router/index.js` 中添加 `/never-have-i-ever` 路由。
4. **入口添加**: 在 `src/views/Home.vue` 中添加“我有你没有”游戏的入口卡片。
5. **文档说明**: 在最终回复中说明如何手动编辑题库（即编辑 `src/data/neverHaveIEver.js` 文件）。

